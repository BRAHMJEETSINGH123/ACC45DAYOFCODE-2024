# Input number of test cases
T = int(input())

# Loop through each test case
for _ in range(T):
    # Input the amount of pulp in kg
    N = int(input())
    
    # Calculate the number of notebooks
    notebooks = N * 10
    
    # Output the result
    print(notebooks)
